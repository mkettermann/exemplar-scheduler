{
  /* Só checagem de tipos: o build de produção ignora `test/` de propósito.
     Ver docs/01-typescript-e-build.md */
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "rootDir": ".",
    "types": ["node"]
  },
  "include": ["src/**/*.ts", "test/**/*.ts"]
}
